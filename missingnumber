class Solution {
    int missingNum(int arr[]) {
        long sum = 0;
        int n = arr.length + 1;

        for(int i = 0; i < arr.length; i++){
            sum += arr[i];
        }

        long total = (long)n * (n + 1) / 2;
        return (int)(total - sum);
    }
}
